{% extends 'base.html.twig' %}

{% block title %}Purchase a product{% endblock %}

{% block body %}
    <h1>Purchase a product</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Purchase</button>
    {{ form_end(form) }}
{% endblock %}
